// Vermont — single-code state in the original 1947 plan.
import type { StateRegionList } from './types';

const regions: StateRegionList = [
  { code: '802', established: 1947 },
];

export default regions;
