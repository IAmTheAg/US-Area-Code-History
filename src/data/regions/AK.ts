// Alaska — 907, its one and only code, in service from 1957. Before that
// AK genuinely had no area code, so it renders gray pre-1957.
import type { StateRegionList } from './types';

const regions: StateRegionList = [
  { code: '907', established: 1957 },
];

export default regions;
